'use client';
import {useEffect,useState} from 'react';
const api=(body:any)=>fetch('/api/creator',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}).then(async r=>({ok:r.ok,data:await r.json()}));
export default function CreatorPage(){const [password,setPassword]=useState('');const [logged,setLogged]=useState(false);const [target,setTarget]=useState('');const [user,setUser]=useState<any>();const [amount,setAmount]=useState('');const [note,setNote]=useState('');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);const [board,setBoard]=useState<any[]>([]);const notify=(x:string)=>{setMsg(x);setTimeout(()=>setMsg(''),3500)};async function login(){setBusy(true);try{const r=await api({action:'creator-login',username:'khoungbell7777',password});if(!r.data?.ok)return notify('Sai mật khẩu Creator.');setLogged(true);loadBoard();notify('✓ Creator đăng nhập thành công.')}finally{setBusy(false)}}async function loadBoard(){try{const r=await fetch('/api/creator?action=leaderboard');const d=await r.json();setBoard(Array.isArray(d.leaderboard)?d.leaderboard:[])}catch{}}async function resolve(){if(!target.trim())return notify('Nhập username Roblox.');setBusy(true);try{const r=await api({action:'resolve',username:target.trim()});if(!r.data?.ok)return notify('Không tìm thấy username Roblox.');setUser(r.data.user)}finally{setBusy(false)}}async function record(){if(!user)return;const n=Number(amount);if(!Number.isInteger(n)||n<=0)return notify('Nhập số Robux donate hợp lệ.');setBusy(true);try{const r=await api({action:'record-donation',userId:user.id,username:user.name,amount:n,note,password});if(!r.data?.ok)return notify(r.data?.error||'Không ghi nhận donation.');notify(`✓ Đã xác nhận donation ${n} Robux của ${user.name}.`);setAmount('');loadBoard()}finally{setBusy(false)}}async function grant(){if(!user)return;setBusy(true);try{const r=await api({action:'grant',username:user.name,password});if(!r.data?.ok)return notify(r.data?.error==='donation_required'?'Chưa có donation được Creator xác nhận.':r.data?.error||'Không cấp Plus được.');notify(`✓ Đã cấp Plus cho ${user.name} (${r.data.donatedRobux||0} Robux donate).`);loadBoard()}finally{setBusy(false)}}const css=`*{box-sizing:border-box}body{margin:0}main{min-height:100vh;padding:22px;background:radial-gradient(circle at 10% 0,#e8e4ff,transparent 35%),#f7f8fc;color:#151622;font-family:Inter,system-ui,sans-serif}.wrap{max-width:920px;margin:auto}.top{display:flex;justify-content:space-between;gap:14px;align-items:center;flex-wrap:wrap}.brand{font-size:27px;font-weight:950}.muted{color:#717589}.card{background:#fff;border:1px solid #e7e7ef;border-radius:22px;padding:22px;margin-top:16px;box-shadow:0 15px 50px #22205a12}.input{width:100%;padding:13px;border:1px solid #d9d9e5;border-radius:12px;margin:7px 0 12px;font:inherit}.btn{border:0;border-radius:12px;padding:12px 16px;font-weight:900;cursor:pointer;background:#181827;color:#fff}.primary{background:linear-gradient(135deg,#5b4bd6,#9b6cff)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:15px}.profile{background:#faf9ff;border-radius:15px;padding:14px;margin-top:14px}.rank{display:flex;justify-content:space-between;padding:11px 0;border-bottom:1px solid #eee}@media(max-width:650px){main{padding:14px}.grid{grid-template-columns:1fr}}`;
if(!logged)return <main><style>{css}</style><div className="wrap"><div className="brand">RBLXFinder Creator</div><div className="muted">Creator-only control center</div><section className="card"><h2>Creator Login</h2><p className="muted">Creator: <b>khoungbell7777</b></p><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mật khẩu Creator"/><button className="btn primary" disabled={busy} onClick={login}>{busy?'Đang kiểm tra...':'Đăng nhập'}</button></section></div>{msg&&<div className="msg">{msg}</div>}</main>;
return <main><style>{css}.msg{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:#171722;color:white;padding:12px 18px;border-radius:999px;font-weight:800}</style><div className="wrap"><div className="top"><div><div className="brand">Creator Dashboard</div><div className="muted">Donation → Plus management</div></div><button className="btn" onClick={()=>{setLogged(false);setPassword('')}}>Đăng xuất</button></div><section className="card"><h2>💎 Xác nhận donation & cấp Plus</h2><p className="muted">Quy trình mới: người dùng <b>phải donate Robux cho {`khoungbell7777`}</b>. Creator xác nhận số Robux đã nhận, sau đó mới cấp Plus. Không còn ownership verification bằng Description.</p><input className="input" value={target} onChange={e=>setTarget(e.target.value)} placeholder="Roblox username đã donate"/><button className="btn" onClick={resolve}>Tìm tài khoản</button>{user&&<div className="profile"><b>{user.name}</b><div className="muted">User ID: {user.id}</div><input className="input" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="Số Robux đã donate" inputMode="numeric"/><input className="input" value={note} onChange={e=>setNote(e.target.value)} placeholder="Ghi chú (không bắt buộc)"/><button className="btn" disabled={busy} onClick={record}>✓ Xác nhận donation</button><button className="btn primary" style={{marginLeft:8}} disabled={busy} onClick={grant}>⭐ Cấp Plus</button></div>}</section><section className="card"><h2>🏆 Bảng xếp hạng Donate</h2>{board.length?board.map((x,i)=><div className="rank" key={x.roblox_user_id}><span><b>#{i+1}</b> &nbsp;{x.roblox_username}</span><b>{x.total_robux} Robux</b></div>):<p className="muted">Chưa có donation.</p>}</section></div>{msg&&<div className="msg">{msg}</div>}</main>}
