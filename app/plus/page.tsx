'use client';
import {useState} from 'react';
const api=(body:any)=>fetch('/api/creator',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}).then(async r=>({ok:r.ok,data:await r.json()}));
export default function Plus(){const [username,setUsername]=useState('');const [user,setUser]=useState<any>();const [code,setCode]=useState('');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);const notify=(x:string)=>{setMsg(x);setTimeout(()=>setMsg(''),3500)};async function find(){if(!username.trim())return;setBusy(true);try{const r=await api({action:'resolve',username});if(!r.data?.ok)return notify('Không tìm thấy username Roblox.');setUser(r.data.user);const s=await api({action:'status',userId:r.data.user.id});if(s.data?.verified)notify('✓ Tài khoản đã Verify Plus.')}finally{setBusy(false)}}async function start(){if(!user)return;setBusy(true);try{const r=await api({action:'start-verification',userId:user.id});if(!r.data?.ok)return notify(r.data?.error||'Bạn chưa được Creator cấp Plus.');setCode(r.data.code)}finally{setBusy(false)}}async function verify(){if(!user||!code)return;setBusy(true);try{const r=await api({action:'verify',userId:user.id,code});notify(r.data?.ok?'✓ Verify thành công! Plus đã mở.':r.data?.error==='code_not_in_profile'?'Chưa thấy mã trong Description Roblox.':r.data?.error||'Verify thất bại.')}finally{setBusy(false)}}const css=`body{margin:0}main{min-height:100vh;background:#f6f7fb;color:#171827;font-family:Inter,system-ui,sans-serif;padding:20px}.wrap{max-width:1050px;margin:auto}.top{display:flex;justify-content:space-between;align-items:center}.brand{font-size:28px;font-weight:950}.hero{background:linear-gradient(135deg,#5545d8,#9b6cff);color:white;border-radius:28px;padding:32px;margin:20px 0}.hero h1{font-size:42px;margin:0 0 8px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px}.card{background:#fff;border:1px solid #e6e7ef;border-radius:20px;padding:20px;box-shadow:0 12px 40px #1718270d}.input{width:100%;box-sizing:border-box;padding:13px;border:1px solid #d9dbe5;border-radius:12px;margin:8px 0 12px;font:inherit}.btn{border:0;border-radius:12px;padding:12px 16px;font-weight:900;background:#171827;color:#fff;cursor:pointer}.primary{background:#6655dd}.code{font:900 25px ui-monospace,monospace;text-align:center;letter-spacing:3px;background:#f1efff;color:#5545cf;padding:17px;border-radius:14px;margin:14px 0}.muted{color:#717589}.hero .muted{color:#eee}.msg{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:#171827;color:#fff;padding:12px 18px;border-radius:999px;font-weight:800}@media(max-width:600px){main{padding:12px}.hero h1{font-size:32px}}`;
return <main><style>{css}</style><div className="wrap"><div className="top"><div className="brand">RBLXFinder</div><a href="/roblox" className="muted">← Roblox</a></div><section className="hero"><h1>👑 RBLXFinder Plus</h1><p className="muted">Power tools cho Roblox + Free Fire. Quyền Plus gắn với Roblox User ID sau khi xác minh.</p></section><div className="grid"><div className="card"><h2>⚡ Server Power</h2><p className="muted">Lọc 0–1, 0–3, 0–5 người · auto refresh · server watcher · favorites · chia sẻ kết quả.</p></div><div className="card"><h2>📊 Game Intelligence</h2><p className="muted">Stats nâng cao · thumbnail · trending · theo dõi game · so sánh game.</p></div><div className="card"><h2>🎯 Free Fire Lab</h2><p className="muted">Preset theo thiết bị · sensitivity calculator · preset battle · cộng đồng.</p></div><div className="card"><h2>🛡️ Priority Tools</h2><p className="muted">Các công cụ Plus mới sẽ được mở dần mà không cần đăng nhập lại mỗi lần.</p></div></div><section className="card" style={{marginTop:16}}><h2>🔐 Verify Plus</h2><p className="muted">Nhập username Roblox đã được Creator cấp Plus. Hệ thống dùng User ID và kiểm tra Description public.</p><input className="input" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Roblox username"/><button className="btn primary" disabled={busy} onClick={find}>Tìm tài khoản</button>{user&&<div style={{marginTop:16}}><b>{user.name}</b><div className="muted">User ID: {user.id}</div><button className="btn" style={{marginTop:12}} disabled={busy} onClick={start}>Tạo mã xác minh</button>{code&&<><div className="code">{code}</div><p className="muted">Dán mã vào Roblox Profile → Description của <b>{user.name}</b>.</p><button className="btn primary" disabled={busy} onClick={verify}>✓ Verify và mở Plus</button></>}</div>}</section></div>{msg&&<div className="msg">{msg}</div>}</main>}
