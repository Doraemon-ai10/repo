import { NextRequest, NextResponse } from 'next/server';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'POST, OPTIONS','Access-Control-Allow-Headers':'Content-Type, Authorization'};
export async function OPTIONS(){return new NextResponse(null,{status:204,headers:cors})}
function answer(game:'roblox'|'freefire',q:string){const x=q.toLowerCase().trim();if(!x)return'Hãy hỏi mình một câu nhé.';if(game==='roblox'){if(/server|sv|ít người|low.?pop|low population/.test(x))return'Muốn tìm server ít người: vào Roblox → Server Finder, nhập Universe ID rồi sắp xếp theo số người. Bản Free vẫn dùng được tìm server công khai và lọc cơ bản; Plus thêm theo dõi server, tự làm mới và bộ lọc nâng cao.';if(/username|user.?id|profile|tài khoản|avatar/.test(x))return'Bạn có thể nhập username Roblox để lấy User ID, hồ sơ cơ bản và avatar công khai. Không cần mật khẩu hay cookie Roblox.';if(/game|trò chơi|game nào|tìm game/.test(x))return'Hãy nhập tên hoặc từ khóa game. Mình có thể giúp bạn chọn game theo thể loại, lượng người chơi và thông tin game.';if(/plus|free|miễn phí/.test(x))return'RBLXFinder Free vẫn giữ các tính năng cốt lõi. Plus tập trung vào công cụ nâng cao.';if(/robux|kiếm robux/.test(x))return'Không có cách hợp pháp nào để tạo Robux miễn phí vô hạn. Tránh website yêu cầu mật khẩu, cookie hoặc tải phần mềm lạ.';return'Mình là Roblox Assistant của RBLXFinder. Bạn có thể hỏi về username, avatar, game, server, Robux hoặc cách dùng RBLXFinder.'}if(/sensitivity|độ nhạy|sens|kéo tâm/.test(x))return'Vào Sensitivity Lab, chọn đúng thiết bị rồi test trong Training.';if(/dpi/.test(x))return'DPI là một phần của cảm giác điều khiển. Giữ DPI ổn định rồi chỉnh sensitivity từng bước.';if(/hud|nút|fire button|nút bắn/.test(x))return'Ưu tiên đặt nút bắn ở vị trí ngón tay chạm tự nhiên và test vài trận.';if(/headshot|one.?tap|hs|bắn đầu/.test(x))return'Luyện kéo tâm đều, giữ FPS ổn định và tinh chỉnh từng bước.';return'Mình là Free Fire Assistant của RBLXFinder. Bạn có thể hỏi về sensitivity, DPI, nút bắn, HUD hoặc headshot.'}
export async function POST(req:NextRequest){try{const b=await req.json();const game=b?.game==='freefire'?'freefire':'roblox';const question=String(b?.message||'').slice(0,1000);return NextResponse.json({game,answer:answer(game,question),reply:answer(game,question),mode:'free'},{headers:cors})}catch{return NextResponse.json({error:'Assistant request failed'},{status:400,headers:cors})}}
