export async function GET(req,{params}){return proxy(req,params)}
export async function POST(req,{params}){return proxy(req,params)}
async function proxy(req,{domain,path}){const hosts={users:'https://users.roblox.com',thumbnails:'https://thumbnails.roblox.com',presence:'https://presence.roblox.com',groups:'https://groups.roblox.com',friends:'https://friends.roblox.com',badges:'https://badges.roblox.com',games:'https://games.roblox.com'};const host=hosts[domain];if(!host)return Response.json({error:'Unsupported Roblox API domain'},{status:400});const url=new URL(`${host}/${path.join('/')}`);new URL(req.url).searchParams.forEach((v,k)=>url.searchParams.set(k,v));try{const body=req.method==='POST'?await req.text():undefined;const r=await fetch(url,{method:req.method,headers:{Accept:'application/json',...(body?{'Content-Type':'application/json'}:{})},body,next:{revalidate:30}});const text=await r.text();return new Response(text,{status:r.status,headers:{'Content-Type':r.headers.get('content-type')||'application/json','Cache-Control':'s-maxage=30, stale-while-revalidate=120'}})}catch{return Response.json({error:'Roblox API request failed'},{status:502})}}
